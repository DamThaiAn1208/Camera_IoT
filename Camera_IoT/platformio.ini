; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:esp32-s3-devkitc-1]
platform = espressif32
board = esp32-s3-devkitc-1
framework = arduino

monitor_speed = 115200
upload_speed = 460800

; Chip thực tế là ESP32-S3-WROOM-1 N16R8: 16MB flash (Quad) + 8MB PSRAM (Octal).
; Board mặc định esp32-s3-devkitc-1 giả định không có PSRAM nên phải override.
board_build.arduino.memory_type = qio_opi
board_build.partitions = default_16MB.csv
board_upload.flash_size = 16MB
board_upload.maximum_size = 16777216

build_flags =
    -DBOARD_HAS_PSRAM
    ; Board không có chip UART-USB rời — route Serial qua cổng
    ; USB-Serial/JTAG tích hợp (cùng cổng COM dùng để nạp) để monitor xem được log.
    -DARDUINO_USB_MODE=1
    -DARDUINO_USB_CDC_ON_BOOT=1

lib_deps =
    espressif/esp32-camera
    knolleary/PubSubClient@^2.8
