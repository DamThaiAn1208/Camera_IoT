#pragma once

// Copy this file to wifi_credentials.h and fill in your network's
// credentials. wifi_credentials.h is gitignored so real credentials
// never get committed.

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
